
import React from 'react';
const ReactDOMServer = require('react-dom/server');

import eot from './Bandwidth.eot';
import { BandwidthProvider } from '@bandwidth/shared-components/dist';
// import { Accordion } from '@bandwidth/shared-components';

// const puppeteer = require('puppeteer');

// class Test extends React.Component {
//   render() {
//     return (
//       <BandwidthProvider>
//         <Accordion label="Hello">
//           Some content
//         </Accordion>
//       </BandwidthProvider>
//       )
//   }
// }

// var html = ReactDOMServer(Test);

// (async () => {
//   const browser = await puppeteer.launch();
//   const page = await browser.newPage();

//   await page.setContent(html);
//   await page.screenshot({path: 'accordion.png'});

//   await browser.close();
// })();
